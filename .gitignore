# Java / Gradle
.gradle/
build/
out/
*.class
bin/
# Node / Vite
node_modules/
dist/
*.log
# Generated frontend bundle (built into the backend's static resources by the frontend build)
/src/main/resources/static/
# Env / secrets
.env
*.local
# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store
/plugins/*
!/plugins/.gitkeep

# Claude Code: shared settings are committed, personal ones are not
# (`*.local` above does not match `settings.local.json`)
.claude/settings.local.json
